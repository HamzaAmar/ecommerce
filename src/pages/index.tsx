import React from 'react';
import type { NextPage } from 'next';
import { Layout } from '@components/common';

const Home: NextPage = () => {
  return (
    <Layout>
      <h1>Hello World</h1>
    </Layout>
  );
};

export default Home;
